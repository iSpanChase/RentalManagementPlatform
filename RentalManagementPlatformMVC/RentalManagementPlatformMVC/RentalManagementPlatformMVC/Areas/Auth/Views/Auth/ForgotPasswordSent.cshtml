@{
    ViewData["Title"] = "重設密碼連結已寄出";
    var link = TempData["ResetLink"] as string;
}
<div class="row justify-content-center mt-4">
    <div class="col-md-8 col-lg-6">
        <div class="card shadow-sm">
            <div class="card-body">
                <h5 class="card-title">已寄出重設密碼連結。</h5>
                <p class="text-muted mb-4">請至信箱收信並在有效時間內完成重設。</p>

                @* @if (!string.IsNullOrEmpty(link))
                {
                    <div class="alert alert-info">
                        <div><strong>開發模式：</strong>以下為產生的測試連結（正式上線請改成寄信！）</div>
                        <div class="mt-2">
                            <a class="btn btn-sm btn-outline-primary" href="@link">直接前往重設密碼</a>
                            <code class="ms-2">@link</code>
                        </div>
                    </div>
                } *@

                <a asp-area="Auth" asp-action="Login" class="btn btn-primary">回登入</a>
            </div>
        </div>
    </div>
</div>
